\input{../main.tex}

\setmathstyle{АУ}{Задание 1}{}


\begin{document}

\libproblem{avg-case-complexity}{avgp-polynomial-reductions}
\libproblem{avg-case-complexity}{avgp-avg-ptime-alg}
\libproblem{cryptography}{owf-poly-image}
\libproblem{cryptography}{owf-xor-x}

\task{
    Задача была утеряна.
}

\breakline


\dzcomment{
    В следующих задачах не помешало бы проверить корректность, добавлять не буду.
}


Будем рассматривать пары $(M, \omega)$, где $M$~--- машина Тьюринга, $\omega$~--- вход для
машины. $\KolmP(x)$~--- длина самой короткой пары $(M, \omega)$, что $M(\omega) = x$.

\task{
    Докажите, что $\sum\limits_{x \in \{0, 1\}^*} 2^{-K(x)} < 1$.
}

Построим семейство распределений
$K_{n}(x) = \frac{2^{-K(x)}}{\sum\limits_{y \in \{0, 1\}^n} 2^{-K(y)}}$

\task{
    Докажите, что $\sum\limits_{y \in \{0, 1\}^n} 2^{-K(y)} = \Omega(\frac{1}{n \log^2(n)})$.
}


Пусть $(L, \{K_n(x)\}) \in \HeurP[\frac{1}{n^3}]$ и $A$~--- алгоритм для этого языка.
$S = \{x \in \{0, 1\}^n \mid A(x) \neq L(x)\}$.

\task{
    Докажите, что:
    а) для любого $x \in S$, $K(x) > 2\log(n) - \log\log(n) - O(1)$ (воспользуйтесь
    тем, что $(L, \{K_n(x)\}) \in Heur_{\frac{1}{n^3}}P$)
    б) из того, что $S \neq \emptyset$ следует, что существует такой $x \in S$, что
    $K(x) < \log(n) + \log\log(n) + O(1)$ (воспользуйтесь определением $K(x)$)
    в) $L\in P$.
}


\end{document}



%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
