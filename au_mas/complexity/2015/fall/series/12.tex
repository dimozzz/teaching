\input{../main.tex}

\setmathstyle{08.12}{}{}

\begin{document}

\setcounter{curtask}{53}

\libproblem{struct-complexity}{am-k-rounds}

\task{
    Покажите, что:
	\begin{enumcyr}
        \item если $\BPTIME[f(n)] = \BPTIME[g(n)]$, то $\BPTIME[f(h(n))] = \BPTIME[g(h(n))]$, где $f, g,
            h$~--- конструктивные по времени, $f(n), g(n) \ge \log n$, $h(n) \ge n$~--- возрастающая функция;
        \item $\DTIME[f(n)] \subseteq \BPTIME[f(n)] \subseteq \DTIME[2^{O(f(n))}]$;
        \item $\BPP \subseteq \BPTIME[n^{\log n}] \subsetneq \BPTIME[2^n]$.
    \end{enumcyr}
}

\dzcomment{
    Не добавлял в базу, в задаче было что-то не так.
}

\libproblem{struct-complexity}{reduction-oracle-3-sat}

\end{document}