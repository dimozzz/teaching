\setfirstnumber{50}

\DB{«адание 10 (на 06.11.13)}

\z $\rm BPL_H$~--- это класс €зыков, дл€ которых существует веро€тностна€ машина “ьюринга $M$, котора€ использует логарифмическую
пам€ть, останавливаетс€ с веро€тностью 1, и дл€ всех $x$ выполн€етс€, что $\Pr[M(x)=L(x)]\ge \frac{2}{3}$. 
ѕокажите, что $\rm BPL_H \subseteq P$.

\PR{ќпределение} язык $L \in \rm MA$, если  существует така€ полиномиальна€ веро€тностна€
машина $V$ и полином $p$, что если $x\in L$, то найдетс€ така€ строка
$y\in \{0,1\}^{p(n)}$, что $\Pr[V(x,y)=1]\ge \frac23$, а если $x \not\in L$, то 
дл€ любой строки $y\in \{0,1\}^{p(n)}$ выполн€етс€ $\Pr[V(x,y)=1]<\frac{1}{3}$.


\z —уществует вариант класса $\rm MA$ с односторонней ошибкой. 
$L\in \rm MA_1$, если существует така€ полиномиальна€ веро€тностна€
машина $V$ и полином $p$, что если $x\in L$, то найдетс€ така€ строка
$y\in \{0,1\}^{p(n)}$, что $\Pr[V(x,y)=1]=1$, а если $x \not\in L$, то 
дл€ любой строки $y\in \{0,1\}^{p(n)}$ выполн€етс€ $\Pr[V(x,y)=1]<\frac{1}{3}$.
ѕокажите, что $\rm MA =MA_1$.

\PR{ќпределение} язык $L \in \rm AM$, если  существует така€ детерминированна€ 
машина $V$ и полином $p$, что если $x\in L$, то 
$\Pr_{z\gets \{0,1\}^{p(n)}}[\exists y\in \{0,1\}^{p(n)}: V(x,z, y)=1]\ge \frac23$, а если 
$x \not\in L$, то 
$\Pr_{z\gets \{0,1\}^{p(n)}}[\exists y\in \{0,1\}^{p(n)}: V(x,z, y)=1]< \frac13$.

\z ѕокажите, что $\rm MA \subseteq AM$.

\z ѕокажите, что $\rm MA \subseteq \Sigma_2^P$.


\bigskip
\hrule
\bigskip

\PRz{9} ћашина “ьюринга называетс€ забывчивой, если положение головки в любой момент времени зависит только от длины входа. ƒокажите, что 
любую машину “ьюринга, работающую врем€ $T(n)$ можно промоделировать за врем€ $O(T^2(n))$ на забывчивой одноленточной машине. 
б) ј на забывчивой двухленточной за врем€ $O(T(n) \log T(n))$.


\PRz{23} ѕокажите, что каждый €зык, который принимаетс€ $k$-ленточной недетерминированной машиной “ьюринга за
врем€ $f(n)$ может быть прин€т $2$-ленточной недетерминорованной машиной за врем€ $O(f(n))$.

 
\PRz{34} ƒокажите, что а) ${\rm DSpace}[n^2]\subsetneq {\rm DSpace}[n^3]$; 
б) ${\rm NSpace}[n^2]\subsetneq {\rm NSpace}[n^3]$.


\PRz{43} ƒокажите, что ${\rm DSpace}[n]\neq \rm NP$.

\PRz{45} ƒокажите, что если унарный €зык $\rm NP$-полный, то $\rm P=NP$.

\PRz{46} ќбозначим UCYCLE множество всех неориентрованных графов, в которых есть
цикл. ƒокажите, что UCYCLE принадлежит классу $\rm L$.

\PRz{47} ƒокажите, что если $\rm NP\subseteq BPP$, то $\rm NP=RP$.
\bigskip