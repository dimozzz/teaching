
\setfirstnumber{1}

\DB{Задание 1 (на 11.09.13)}.

\z Докажите, что существует один такой алгоритм $M$, что множество входов, на котором он останавливается, неразрешимо.

\z Докажите, что непустое подмножество натуральных чисел разрешимо тогда 
и только тогда, когда оно есть множество значений всюду определенной неубывающей 
вычислимой функции с натуральными аргументами и значениями.

\z Даны два пересекающихся перечислимых множества $X$ и $Y$ . 
Докажите, что найдутся непересекающиеся перечислимые множества 
$X'\subset X$ и $Y'\subset Y$ , что $X' \cup Y'$ = $X \cup Y$ .

\z Докажите, что всякое бесконечное перечислимое множество содержит 
бесконечное разрешимое подмножество.

%\z Докажите, что для каждой вычислимой функции $f$ существует псевдообратная вычислимая
%функция $g$. А именно, $g$ определена на множестве значений $f$. 
%И для всех $x$ из области определения $f$
%выполняется $g(f(x)) = f(x)$.

\z Приведите пример множества, которое неперечислимо и дополнение к нему 
тоже неперечислимо.

\z Существует ли алгоритм, проверяющий, работает ли данная программа 
полиномиальное время?

\z Докажите, что не существует алгоритма, который по программе $M$ определил бы, является
ли последовательность $M(1) ,M(2),M(3) \dots$ периодической с некоторого места.

%\z Покажите, что множество перечислимо тогда и только тогда,
%когда оно является областью значений взаимно-однозначной вычислимой
%функции.

\z Обозначим через $K(x)$ минимальный номер алгоритма, который на входе $0$ печатает $x$ и останавливается. 
Докажите, что функция $K(x)$ невычислима.

\z Машина Тьюринга называется забывчивой, если положение головки в любой момент времени зависит только от длины входа. а) Докажите, что 
любую машину Тьюринга, работающую время $T(n)$ можно промоделировать за время $O(T^2(n))$ на забывчивой одноленточной машине. 
б) А на забывчивой двухленточной за время $O(T(n) \log T(n))$.

\bigskip