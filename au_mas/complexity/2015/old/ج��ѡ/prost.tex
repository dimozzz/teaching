\input defs.tex

\sloppy

\begin{document}



\fontsize{12pt}{18pt}

\selectfont


\setfirstnumber{1}

%\DB{Часть 1}

\z Какие определения класса $\rm NP$ Вы знаете?

\z Какую задачу решает универсальный алгоритм Левина? 
Дайте точный ответ.

\z Что такое язык $\Sigma_i$-SAT?

\z Что такое язык $\Pi_i$-SAT? 

\z Дайте определение классам $\rm BPP$ и $\rm RP$.

\z Дайте определение классу $\rm IP$.

\z Дайте определение классам ${\rm AM}[k(n)]$.

\z Сформулируйте теорему Карпа-Липтона.

\z Сформулируйте $\rm PCP$-теорему (с определениями).

\z Приведите пример универсального семейства хеш-функций.

\z Что такое код Уолша-Адамара? 

\z В каком уровне полиномиальной иерархии обязательно есть языки,
которые не решаются схемами размера $n^2$? 

\z Что можно сказать о включениях между классами 
$\rm BPP$, $\rm RP$ и $\rm PP$?

\z Приведите пример языка, который не лежит в $\rm NP$.

\z Какие включения между классами 
$\rm P, EXP, NP, RP, NL, PH, \Sigma_3^P, L, PSPACE$ вы знаете?

\z Какие включения между классами 
$\rm P, EXP, NP, RP, NL, PH, \Sigma_3^P, L, PSPACE$ точно являются строгими?

\z Для какого наименьшего (с точностью до константы) 
$t(n)$ Вы можете доказать включение
${\rm NSPACE}[n^2]\subseteq {\rm DTime}[t(n)]$.

\z Можно ли что-нибудь утверждать про соотношение классов 
$\rm RP$ и $\rm NEXP$?

\z Можно ли что-нибудь утверждать про соотношение классов 
$\rm BPP$ и $\rm NP$?

\z Пусть $\rm NP\subseteq BPP$, что Вы можете сказать 
про полиномиальную иерархию в этом случае?

\z Сравните классы ${\rm PCP}(\log n, \log n)$ и $\rm RP$.

\z Расставьте включения: $\rm NP, MA, PP$. Какие из них точно строгие?

\z Есть ли для языка $\Sigma_i-SAT$ интерактивный протокол?

\z Известно, что язык $L$ рещается 5-ленточной 
$\rm BPP$-машиной Тьюринга за время $n^2$. 
За какое время язык $L$ точно можно решить на 1 ленточной 
детерминированной машине?

\z Что можно сказать о классах ${\rm NSpace[n^2]}$ и $\rm{coNspace}[n^2]$?

\z В какой уровень полиномиальной иерархии Вы можете 
поместить множество графов с нечетным размером максимальной клики?

\z Известно, что китайские математики доказали, что $\rm P=NP$. 
Сможете ли Вы (не разбираясь в доказательстве) 
написать алгоритм для задачи $SAT$, который будет работать полиномиальное время?

\z Известно, что у машины Тьюринга $q$ состояний, 
алфавит состоит из $c$ символов и на входах длины $n$ она использует
$s(n)$ ячеек памяти. Как оценить сверху число конфигураций у такой машины? 

\z Приведите пример языка, который не лежит в классе $\rm IP$.

\z Можно ли построить семейство схем полиномиального размера, которые бы
проверяли бы числа на простоту? Почему?

\z В какие известные Вам классы сложности попадает задача $GNI$ 
(из вкладывающихся друг в друга классов можно выбирать наименьший)?

\z Разделите классы на ``простые'' и ``сложные'': 
$\rm P, RP, PP, \sharp P$, $\rm NP, PSPACE, BPP, L, NL, EXP, PSPACE, PH$.

\z Петя утверждает, что пользуясь оракулом $SAT$, он может за полиномиальное
время считать число выполняющих наборов формулы. Что из этого следует про 
полиномиальную иерархию?

\z Для какого $t(n)$ Вы можете доказать
включение ${\rm RPTime}[n^2]\subseteq {\rm DTime}[t(n)]$?

\z Существует ли булева функция $\{0,1\}^n\to \{0,1\}$, 
которая не решается схемами размера меньше, чем $3^n$?

\z Приведите пример неразрешимого языка, 
который лежит в классе $\rm P/poly$.

\z Существует ли такой оракул $A$, что $\rm P^A=RP^A$?

\z Какие классы Вы знаете, которые замкнуты относительно дополнения?

\z Приведите пример разрешимого языка, который не лежит в классе $\rm RP$.

\z Рассмотрим язык неравенств с целыми коэффициентами над 0/1 переменными. Лежит ли этот язык в классе $\rm NP$?
Является ли он $\rm NP$-полным? 

\z С каким известным классом совпадает класс ${\rm PCP}(1,1)$?

\z С каким известным классом совпадает класс ${\rm PCP}(\log\log n,1)$?

\z Сколько потребуется параллельных тактов для проверки, что между двумя вершинами в неориентированном графе из $n$ вершин есть путь?

\z Сколько нужно памяти для проверки, что между двумя вершинами в ориентированном графе из $n$ вершин есть путь?


\end{document}