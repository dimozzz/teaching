\input{defs}

\setfirstnumber{1}

\begin{document}

\fontsize{12pt}{16pt}
\selectfont


\DB{Вопросы к экзамену по курсу ``Теория сложности вычислений'', АУ 2013}

\z Машины Тьюринга. Нижняя оценка для палиндрома на одноленточной машине.

\z Многоленточные машины Тьюринга. Универсальная машина Тьюринга. 
Эффективное моделирование $k$-ленточной машины на 
$2$-ленточной.

\z Недетерминированные машины Тьюринга. Определения класса $\rm NP$. Примеры.

\z Сведения по Карпу. Понятние полноты. Полнота задачи об ограниченной остановке.
Полнота задачи $SAT$ и 3-$SAT$.

\z $\rm NP$-задачи поиска. Сведения по Куку. Сведение задач поиска к задачам распознавания.

\z Оптимальный алгоритм для $\rm NP$-задач поиска.

\z Теорема Ладнера о не $\rm NP$-полном языке в классе $\rm NP$.

\z Классы с оракулами. Существование оракулов $A,B$, при которых 
${\rm P}^A={\rm NP}^A$ и ${\rm P}^B\neq {\rm P}^B$. 

\z Теоремы об иерархии по времени для детерминированных и недетерминированных 
вычислений.

\z Вычисления с ограничением по памяти. Теорема Савича и следствие о 
$\rm NPSPACE=PSPACE$.

\z Полнота $TQBF$ в классе $\rm PSPACE$.

\z Логарифмические по памяти сведения и их свойства.
Класс $\rm NL$, полная задача в нем. Замкнутость классов ${\rm NSpace}[s(n)]$
относительно дополнения.

\z Полиномиальная иерархия. Простейшие свойства, полные задачи в $\Sigma_i^P$
и $\Pi_i^P$.

\z Оракульное определение полиномиальной иерархии.

\z Альтернирующие машины Тьюринга и полиномиальная иерархия. 

\z Вычисления с ограничением по времени и по памяти. Нижняя оценка для 
$SAT$.

\z Булевы схемы. Вычисления с подсказкой. Класс $\rm P/poly$. Включение 
$\rm P\in P/poly$. Существование функций большой схемной сложности.

\z Теорема Карпа-Липтона. 

\z Языки большой схемной сложности в полиномиальной иерархии (теорема Каннана).

\z Равномерные схемы. Классы $\rm NC^i$. $\rm P$-полные задачи. 
Соотношение между $\rm NC^1, L, NL$ и $\rm NC^2$. Замкнутость 
$\rm NC$ относительно логарифмических по памяти сведений. 

\z Эффективные параллельные схемы для сложения и умножения чисел.

\z Вероятностная машина Тьюринга. Классы $\rm BPP$ и $\rm RP$.
Лемма Шварца-Зиппеля и вероятностный тест равенства двух многочленов.

%\z Вероятностный алгоритм проверки числа на простоту.

\z Понижение ошибки в классе $\rm BPP$, $\rm BPP \subseteq P/poly$,
$\rm BPP\subseteq \Sigma_2^P\cap \Pi_2^p$.

\z Интерактивные протоколы. Примеры: интерактивный протокол для неизоморфизмов 
графов, $\rm IP\subseteq PSPACE$.

\z Теорема Шамира ($\rm PSPACE\subseteq IP$) и ее следствия. 

\z Универсальное семейство попарно независимых хеш-функций. Конструкция.

\z Протокол для нижней оценки на размер множества. Открытые и закрытые 
случайные биты, игры Мерлина и Артура.
$GNI \in \rm AM$. Теорема Голдвассера-Сипсера (без доказательства).
Если для $\rm GI$ является $\rm NP$-полным, то полиномиальная иерархия схлопывается на втором уровне.

% Классы $\rm UP$ и $\bigoplus P$. 
\z Лемма Вэлианта-Вазирани. 
И ее $\oplus$-версия.

\z Операции с числом выполняющих наборов и следствия из них. 
$\oplus$-версия леммы Вэлианта-Вазирани для полиномиальной иерархии.

\z Классы $\rm \sharp P$ и $\rm PP$. Теорема Тода. $\rm P^{PP}=\rm P^{\sharp P}$.

%\z Теорема Вэлианта о $\sharp P$-полноте 0/1 перманента.

\z Теорема Вэлианта о $\sharp P$-полноте 0/1 перманента (без доказательства).
Интерактивное доказательство для перманента. 
Следствие об интерактивном доказательстве для $\rm P^{PP}$.

\z Включение $\rm MA \subseteq PP$. Схемная сложность $\rm PP$.

\z Приближенные алгоритмы для задачи $\rm MAXSAT$ и минимальном вершинном 
покрытии. Вероятностно проверяемые доказательства. Формулировка 
$\rm PCP$-теоремы. Эквивалентные формулировки. Неаппроксимируемость 
$MAX{-}3{-}SAT$. 

\z Код Уолша-Адамара и его локальное декодирование.

\z $\rm NP\subseteq PCP(poly,1)$.

\z Тестирование функции на гомоморфизм в абелевых группах.

\z Тестирование и восстановление многочленов небольшой степени над полем $\mathbb{F}_p$.

\z ${\rm MIP=PCP}(poly,poly)$,  $\rm MIP \subseteq NEXP$. 

\z $\rm NEXP \subseteq MIP$.




%\newpage

\DB{Требования к экзамену} 

Экзамен состоит из трех частей:

\begin{enumerate}
\item В первой части экзамена требуется ответить на два вопроса из списка вопросов. 
Для получения оценки хотя бы 4 требуется полностью сформулировать основные результаты из двух 
вопросов и провести все доказательства, 
для получения оценки 3 в одном из двух вопросов разрешается не доказать незначительное верное 
утверждение, при том, что общий план доказательства верен.
Пользоваться во время подготовки можно только официальной собственноручно 
написанной шпаргалкой (на листе A4 c двух сторон) без ограничения времени,
так же можно подсмотреть в конспект или книжку, 
но не более, чем на 15 минут.
\item Во второй части экзамена выдается $6$ простых (дурацких) вопросов на знание и понимание. 
Примеры вопросов:
а) сформулируйте определение класса $\rm IP$; б) Почему из того, что 
$\rm NP=NEXP$ следует, что полиномиальная иерархия не схлопывается?
На оценку $t$ необходимо ответить на не менее, чем $t$ вопросов. Пользоваться
источниками информации запрещено.
\item Третья часть экзамена только для тех, кто претендует на оценку 5.
На оценку 5 необходимо решить задачу. Можно пользовать конспектом и 
книжкой, но решать нужно самостоятельно.
\item Итоговая оценка: максимальная оценка, для которой выполнены условия пунктов
1, 2, 3.  
\end{enumerate}

\end{document}