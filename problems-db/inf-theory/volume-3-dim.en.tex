For a set $A \subseteq \mathbb{N}^3$ let us denote as $\pi_{ij}$ the projection onto the plane
generated by coordinate axis $i, j \in [3]$. Show that for any finite $A$:
$$
    2 \log |A| \le \log |\pi_{12}(A)| + \log |\pi_{13}(A)| + \log |\pi_{23}(A)|.
$$