\task{
    Приведите пример таких распределений $\alpha$ и $\beta$, что выполнены все перечисленные свойства:
    \begin{itemize}
        \item $\entropy(\alpha) = I(\alpha : \beta) + \entropy(\beta)$;
        \item $\entropy(\beta) > 0$;
        \item $I(\alpha : \beta) > 0$,
    \end{itemize}
    или покажите, что их не существует.
}
