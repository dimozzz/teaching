\task{
    Пусть $\Kolm(x) = s$, оцените $\Kolm(x x^1 x^2 x^4 x^{8} \dots x^{\ell})$, где $x^i$~--- строка $x$ с
    инвертированным битом на позиции $i$, а $\ell$ ближайшая степень двойки к длине $x$.
}    
