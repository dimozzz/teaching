\input{defs.tex}

\renewcommand{\coursetitle}{\textsc{TCS}}
\setcounter{curtask}{1}
%\teachermode

\begin{document}
\selectlanguage{russian}

\setmathstyle{}{Тест по теоретической информатике}{}

\task{
    Замкнуто ли множество языков, задаваемых (контекстно-свободной) грамматикой, относительно
    объединения.
    \answer{да}
}
% да

\task{
    Распознается ли язык $L \coloneqq \{a^{3n} b^{3m} \mid n, m \in \mathbb{N}\}$ детерминированным
    конечным автоматом?
    \answer{да}
}
%да

\task{
    Является ли язык $L \coloneqq \{a^{3^n}b^{3^m} \mid n, m \in \mathbb{N}\}$ регулярным?
    \answer{нет}
}
%нет

\task{
    Является ли множество машин Тьюринга, останавливающихся на каком-либо входе, перечислимым?
    \answer{да}
}
% да

\task{
    Верно ли, что любое подмножество разрешимого множества является разрешимым?
    \answer{нет}
}
% нет

\task{
    Является ли множество машин Тьюринга, останавливающихся на каком-либо входе, разрешимым?
    \answer{нет}
}
% нет

\task{
    Верно ли, что $\coNP \subseteq \PSPACE$?
    \answer{да}
}
%да

\task{
    Верно ли, что $\RP \subseteq \EXP$?
    \answer{да}
}
%да

\task{
    У двух шестигранных кубиков, грани помечены числами от $1$ до $6$. Энтропия какого из распределений
    больше: <<честный>> кубик; кубик выпадающий только четными числами (с равной вероятностью)?
    \answer{честный}
}
%честный

\task{
    Пусть $\alpha$~--- равномерное распределение на числах $\{1, 2, 3, 4\}$, а $\beta + \alpha = 5$. Чему
    равна условная энтропия $\entropy(\beta | \alpha)$?
    \answer{0}
}
%0



\end{document}

