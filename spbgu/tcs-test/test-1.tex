\input{defs.tex}

\renewcommand{\coursetitle}{\textsc{TCS}}
\setcounter{curtask}{1}

\begin{document}
\selectlanguage{russian}

\setmathstyle{08.04.2021}{Тест по теоретической информатике}{}

\task{
    Распознается ли язык $L \coloneqq \{a^nbc^n \mid n \in \mathbb{N}\}$ детерминированным конечным автоматом?
}
%нет
\task{
    Можно ли задать язык $L \coloneqq \{a^nbc^n \mid n \in \mathbb{N}\}$ (контекстно-свободной) грамматикой?
}
% да

\task{
    Является ли язык $L \coloneqq \{a^nb^m \mid n, m \in \mathbb{N}\}$ регулярным?
}
%да

\task{
    Является ли множество нигде не останавливающихся машин Тьюринга разрешимым?
}
% нет

\task{
    Верно ли, что дополнение любого перечислимого множества является перечислимым?
}
% нет

\task{
    Является ли множество нигде не останавливающихся машин Тьюринга перечислимым?
}
% нет

\task{
    Верно ли, что $\NP \subseteq \EXP$?
}
%да

\task{
    Верно ли, что $\BPP \subseteq \EXP$?
}
%да

\task{
    Энтропия какого из распределений больше: <<честная>> монетка; монетка, выпадающая орлом с вероятностью
    $\frac{3}{4}$?
}
%честной

\task{
    Пусть $\alpha$~--- равномерное распределение на числах $\{0, 1, 2, 3\}$, а $\beta = \alpha + 1$. Чему
    равна взаимная информация в $\alpha$ и $\beta$?
}
%2




\end{document}

